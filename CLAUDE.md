# CLAUDE.md

Read `AGENTS.md` at the start of every session — it holds this repo's
conventions (versioning, workflow, dependencies, product model, etc.) and they
are binding. When a convention changes, update `AGENTS.md` in the same change.
